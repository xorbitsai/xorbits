name: Build Sphinx Documentation

on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade --no-cache-dir pip setuptools
          python -m pip install --upgrade --no-cache-dir sphinx readthedocs-sphinx-ext
          python -m pip install --upgrade --upgrade-strategy only-if-needed --no-cache-dir ./python

        #   python -m pip install matplotlib
        #   python -m pip install datasets
        #   python -m pip install pydata-sphinx-theme>=0.3.0 sphinx-intl>=0.9.9
        #   python -m pip install sphinx sphinx-rtd-theme

      - name: Build Sphinx HTML
        run: |
          python -m sphinx -T -b html -d _build/doctrees -D language=en . ${{ github.workspace }}/doc/_build/html
        working-directory: ./doc/source

